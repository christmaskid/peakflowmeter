name: peakflowmeter
description: "A peak flow meter app for tracking respiratory health."
publish_to: "none"
version: 0.1.3

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  fl_chart: ^1.1.0 # Use the latest version available
  path: ^1.8.3
  path_provider: ^2.1.3
  open_file: ^3.3.2
  drift: ^2.28.1
  drift_flutter: 0.2.6 # Match drift version
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.28.1     # Match drift version
  build_runner: ^2.4.8
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true