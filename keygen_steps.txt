// https://docs.flutter.dev/deployment/android#create-an-upload-keystore

wsl
keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
mv /root/upload-keystore.jks secrets/
exit